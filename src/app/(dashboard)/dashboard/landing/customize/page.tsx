import { LiveCustomization } from '@/components/landing-builder/live-customization';

export const metadata = {
  title: 'Customize Landing Page - Live Editor',
  description: 'Customize your landing page with real-time preview',
};

export default function CustomizePage() {
  return <LiveCustomization />;
}
